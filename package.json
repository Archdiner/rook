{
  "name": "rook",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "zybit"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=zybit",
    "build": "npm run build --workspace=zybit",
    "start": "npm run start --workspace=zybit",
    "lint": "npm run lint --workspace=zybit"
  }
}
